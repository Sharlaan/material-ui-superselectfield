import { func, node, object, string } from 'prop-types';

export default {
  dropDownIcon: node,
  errorStyle: object,
  errorText: string,
  hintText: string,
  selectionsRenderer: func,
  underlineErrorStyle: object,
};
